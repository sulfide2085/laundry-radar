services:
  laundry-radar:
    image: laundry-radar:latest
    container_name: laundry-radar
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
